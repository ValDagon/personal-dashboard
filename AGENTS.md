# AGENTS.md — three-worlds

> Карта репо, не энциклопедия. Сначала этот файл → `docs/PROGRESS.md` (шапка + хвост журнала).
> Crystal-skills глобальные (`~/.cursor/skills`). Не копировать их сюда.

## 0.1 NOW

> **Сейчас:** класс L. Спека `docs/10-product-spec.md` в **review**. UI приложения не писать до «утверждаю».
> **Не делать:** секреты / `.env` в git; публичный GitHub до гейта анонимизации; Next.js/Relomap-стек; четвёртая колонка; код в vault `/Users/valentin/Work/base`; коммит без просьбы после первого scaffold.

| | |
|---|---|
| **Live** | Python 3.12+ · pandas · DuckDB · Streamlit · Plotly · YAML/CSV. Агент делает, человек гейтит. |
| **Next** | Человек: «утверждаю» спеку (или правки). Потом implement. |
| **Проверка** | `pytest` (слой данных). UI: `streamlit run app.py` — после гейта. |

## 1. Миссия

Публичное портфолио-приложение: три мира (Freelance / Work / Hobby) на BI-стеке найма. Память цикла — `docs/PROGRESS.md`. Vault Crystal — отдельный чат, не мульти-root.

## 2. Старт сессии

1. Прочитай §0.1 и хвост `docs/PROGRESS.md`.
2. Нетривиальная задача → `discover-skills-mcp`, затем `crystal-pipeline`.
3. UI → `crystal-frontend-ui` + `hallmark` внутри лимитов Streamlit (кастомный CSS, не дефолтный хром).
4. Перед кодом Streamlit: skill `developing-with-streamlit` (streamlit/agent-skills) — поставить после «ставь».
5. Не отправлять человека «напиши код сам».

## 3. Карта

| Путь | Зачем |
|---|---|
| `docs/PROGRESS.md` | журнал сессий |
| `docs/10-product-spec.md` | спека · review |
| `docs/research/` | синтез внешних источников |
| `data/projects.yaml` | SSOT карточек (без секретов) |
| `.cursor/rules/` | законы этого репо |
| README | как запускать глазами нанимателя |

## 4. Инварианты

1. Нет секретов, нет чтения `.env`.
2. Коммит только если человек попросил (кроме первого scaffold).
3. Готовое (skill/MCP) важнее самописного.
4. Остаёмся в **этом** корне. Исключение: зеркало `~/.cursor/skills`.
5. Нельзя трогать: `.env`, настоящие имена заказчиков до гейта, vault `base`, четвёртая «жизнь».
6. Один эксперимент — одна переменная.
7. Три мира никогда не сливаются в один фид.

## 5. Конец сессии

Строка в конец журнала `docs/PROGRESS.md`. Человеку: сделано / блокер / next. Harvest skill, если цикл повторится.

*Следующий агент увидит только то, что записано.*
